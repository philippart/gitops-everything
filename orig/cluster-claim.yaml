apiVersion: nokia.com/v1alpha1
kind: ClusterClaim
metadata:
  name: team-a
  labels:
    cluster-owner: laurent
spec:
  id: team-a
  compositionSelector:
    matchLabels:
      provider: aws
      cluster: eks
  parameters:
    # Possible values small, medium, large
    nodeSize: small
    # version: "1.21"
    # minNodeCount: 1
